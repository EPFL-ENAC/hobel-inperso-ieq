# Run Docker compose stack

Create a `.env` file with the following variables:

```
GRAFANA_USER=...
GRAFANA_PASSWORD=...
INFLUX_NAME=...
INFLUX_PASSWORD=...
INFLUX_TOKEN=...
```

Then run

```
docker compose up -d
```
