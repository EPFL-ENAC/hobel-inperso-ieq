# Run Docker compose stack

Create a `.env` file with the following variables:

```
GRAFANA_USER=...
GRAFANA_PASSWORD=...
INFLUXDB_USER=...
INFLUXDB_PASSWORD=...
```

Then run

```
docker compose up -d
```
